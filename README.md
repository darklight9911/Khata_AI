# খাতা AI — Khata AI

Photograph a handwritten Bangladeshi credit ledger, Gemini extracts the entries,
you correct anything uncertain, then listen to your receivables in Bangla.

Set `GEMINI_API_KEY` and `ELEVENLABS_API_KEY` in `.env.local`, then `npm run dev`.
